== Project: Build a data scraper to get data from property website


= Sample Querry Graphql

{
  properties(name: "Exo Apartments"){
    name    
    websiteUrl
    address
    time
  }
}

{
  properties(name: "Carraway"){
    name    
    websiteUrl
    address
    time
  }
}


= Sample Mutation Graphql

mutation {
  scrapeProperty (
    name: "exo reston"
  ) {
	    name    
  }
}

mutation {
  scrapeProperty (
    name: "livecarraway"
  ) {
	    name    
  }
}
